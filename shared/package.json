{
  "name": "@placement-platform/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types for the Placement Platform",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
